<%= simple_form_for (@service) do |f| %>
  <strong><%= f.input :type, placeholder: "Service" %></strong>
  <strong><%= f.input :photo %></strong>
  <strong><%= f.input :description, placeholder: "Hair cut" %></strong>
<% end %>
